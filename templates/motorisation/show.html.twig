{% extends 'base.html.twig' %}

{% block title %}Motorisation{% endblock %}

{% block body %}
    <h1>Motorisation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ motorisation.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ motorisation.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_motorisation_index') }}">back to list</a>

    <a href="{{ path('app_motorisation_edit', {'id': motorisation.id}) }}">edit</a>

    {{ include('motorisation/_delete_form.html.twig') }}
{% endblock %}
